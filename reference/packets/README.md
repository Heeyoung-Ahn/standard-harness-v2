# Packets

This directory stores task-level approval packets and packet templates used by the live harness.

For a fresh starter, only the generic `PKT-01_WORK_ITEM_PACKET_TEMPLATE.md` is shipped by default.
Concrete project packets should be created here only after kickoff planning is closed.
