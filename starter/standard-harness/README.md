# Standard Harness Starter

This directory is the clean starter payload for Standard Harness v2.

It must contain only starter-safe onboarding and template files. It must not contain local
state, local evidence, logs, caches, secrets, external review attachments, or generated
validation reports from this development repository.
